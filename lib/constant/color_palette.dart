import 'package:flutter/material.dart';

class ColorPalette {
  static ColorScheme colorScheme = const ColorScheme.light(
    primary: primary,
    secondary: secondary,
  );
  static const Color primary = Color(0xff8F00FF);
  static const Color secondary = Color(0xff8F00FF);
}